# Github repos
 services:
  sample-repo:
    service_id: github
    parameters:
      repo_name: '{{toolchain.name}}'
      repo_url: 'https://github.com/rapchic/IBM-Cloud-simple-quiz.git'
      type: clone
      has_issues: true
      enable_traceability: true
